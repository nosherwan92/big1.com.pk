# Local preview config — contains absolute Windows paths, not portable
.claude/

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
*.swp

# Internal handover notes — keep local, do not publish to the public repo
HANDOVER.md
